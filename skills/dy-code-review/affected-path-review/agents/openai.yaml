interface:
  display_name: "Affected Path Review"
  short_description: "Review affected behavior paths, not just changed lines."
  default_prompt: "Use affected-path-review only when the user explicitly asks to use it or explicitly asks for a full behavior-path review."

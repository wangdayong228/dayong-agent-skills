interface:
  display_name: "Affected Path Review"
  short_description: "Review affected behavior paths, not just changed lines."
  default_prompt: "Use affected-path-review only after a Superpowers Plan has completed every Task and a whole-branch/final code review is starting, or when the user explicitly asks to use it."

interface:
  display_name: "Iterative Code Review"
  short_description: "Parallel main-agent + context-packed review loop"
  default_prompt: "Use $iterative-code-review to run a bounded parallel review loop with context-packed subagents and fix verified issues."

interface:
  display_name: "Strict API Extraction"
  short_description: "Collect official API docs into source/raw and source/snapshots without guessing."
  default_prompt: "Use strict-api-extraction to collect all official API documentation pages with ego-browser (primary) and Firecrawl (auxiliary), continuing discovery until coverage is satisfied and never guessing undocumented elements."
